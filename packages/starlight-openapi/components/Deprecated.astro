---
import { Aside } from '@astrojs/starlight/components'

interface Props {
  deprecated: boolean | undefined
}

const { deprecated } = Astro.props
---

{
  deprecated && (
    <Aside type="caution" title="This operation is deprecated">
      We strongly advise you to use a newer operation if possible.
    </Aside>
  )
}
